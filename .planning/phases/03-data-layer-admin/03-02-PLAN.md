---
phase: 03-data-layer-admin
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/lib/blob.ts
  - src/lib/image.ts
  - src/types/portfolio.ts
  - next.config.ts
  - src/app/api/upload/route.ts
  - src/app/admin/upload/page.tsx
autonomous: true
requirements:
  - UPLD-01
  - UPLD-02
  - UPLD-03
  - UPLD-04
  - UPLD-05
  - UPLD-06

must_haves:
  truths:
    - "`src/app/api/upload/route.ts` accepts authenticated multipart form uploads and writes both Blob + `portfolio_items` metadata"
    - "`src/lib/image.ts` provides a client-side resize helper that reduces images to max 2000px before upload"
    - "`src/lib/blob.ts` validates image file size/type before calling `put()`"
    - "`src/app/admin/upload/page.tsx` exposes metadata fields plus web/PDF usage toggles and progress UI"
    - "`next.config.ts` is ready to render Vercel Blob-hosted images"
  artifacts:
    - path: "src/lib/blob.ts"
      provides: "Blob upload helper for public admin assets"
    - path: "src/lib/image.ts"
      provides: "Client-side image resize helper"
      exports: ["resizePortfolioImage"]
    - path: "src/app/api/upload/route.ts"
      provides: "Authenticated portfolio upload endpoint"
    - path: "src/app/admin/upload/page.tsx"
      provides: "Admin upload UI"
  key_links:
    - from: "src/app/api/upload/route.ts"
      to: "src/lib/blob.ts"
      via: "import { uploadPublicImage } from '@/lib/blob'"
      pattern: "uploadPublicImage"
    - from: "src/app/admin/upload/page.tsx"
      to: "src/lib/image.ts"
      via: "import { resizePortfolioImage } from '@/lib/image'"
      pattern: "resizePortfolioImage"
---

<objective>
Build the upload primitive that creates real `portfolio_items` rows from admin input and
stores the corresponding images in Vercel Blob.

Purpose: the rest of Phase 3 depends on having a reliable way to create portfolio data from
the admin interface. This plan owns the image resize, Blob write, DB insert, and feedback
loop in one feature slice.

Output:
- `src/lib/blob.ts`
- `src/lib/image.ts`
- `src/types/portfolio.ts`
- `next.config.ts`
- `src/app/api/upload/route.ts`
- `src/app/admin/upload/page.tsx`
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-data-layer-admin/03-RESEARCH.md
@scripts/schema.sql
@src/lib/db.ts
@src/lib/blob.ts
@next.config.ts
@node_modules/@vercel/blob/README.md

<interfaces>
upload_form_fields:
  file: File
  title: string
  brandName: string
  celebrityName: string
  category: string
  showOnWeb: "true" | "false"
  showOnPdf: "true" | "false"
allowed_image_types:
  - image/jpeg
  - image/png
  - image/webp
max_server_upload_size_bytes: 4500000
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Blob upload validation, resize helper, and shared portfolio types</name>
  <files>src/lib/blob.ts, src/lib/image.ts, src/types/portfolio.ts, next.config.ts</files>
  <read_first>
    - src/lib/blob.ts
    - next.config.ts
    - scripts/schema.sql
    - node_modules/@vercel/blob/README.md
    - .planning/phases/03-data-layer-admin/03-RESEARCH.md
  </read_first>
  <action>
Create upload primitives with these exact rules:

1. `src/types/portfolio.ts`
   - export a `PortfolioItemRecord` type matching `portfolio_items`
   - export `PORTFOLIO_CATEGORIES = ['상의', '하의', '신발', '악세서리', '기타'] as const`
2. `src/lib/blob.ts`
   - export `async function uploadPublicImage(file: File, pathnamePrefix: 'portfolio' | 'brands'): Promise<{ url: string; pathname: string }>`
   - reject files over `4_500_000` bytes
   - reject MIME types outside `image/jpeg`, `image/png`, `image/webp`
   - upload to `${pathnamePrefix}/${Date.now()}-${sanitizedName}` with `put()`
   - use `access: 'public'` and `addRandomSuffix: true`
3. `src/lib/image.ts`
   - export `async function resizePortfolioImage(file: File): Promise<File>`
   - resize image on the client to max width or height `2000`
   - output a JPEG `File` with quality `0.8`
4. `next.config.ts`
   - add a remote pattern for public Vercel Blob URLs using hostname `**.public.blob.vercel-storage.com`

Do not implement client upload tokens or multipart upload orchestration in this phase.
  </action>
  <verify>
    <automated>cd /Users/arisnoba/Documents/GitHub/afeelcompany && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/blob.ts` contains `4_500_000`
    - `src/lib/blob.ts` contains `access: 'public'`
    - `src/lib/image.ts` contains `2000`
    - `src/lib/image.ts` contains `0.8`
    - `src/types/portfolio.ts` contains `PORTFOLIO_CATEGORIES`
    - `next.config.ts` contains `public.blob.vercel-storage.com`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Blob upload, client resize, and shared portfolio constants exist for the admin upload flow.</done>
</task>

<task type="auto">
  <name>Task 2: Create authenticated upload endpoint and admin upload page</name>
  <files>src/app/api/upload/route.ts, src/app/admin/upload/page.tsx</files>
  <read_first>
    - src/app/admin/upload/page.tsx
    - src/app/api/upload/route.ts
    - src/lib/auth.ts
    - src/lib/blob.ts
    - src/lib/db.ts
    - src/types/portfolio.ts
    - scripts/schema.sql
  </read_first>
  <action>
Create the upload feature with these exact behaviors:

1. `src/app/api/upload/route.ts`
   - export `async function POST(request: Request)`
   - verify admin session before anything else
   - read `await request.formData()`
   - require keys `file`, `title`, `brandName`, `category`, `showOnWeb`, `showOnPdf`
   - upload the image through `uploadPublicImage(file, 'portfolio')`
   - insert into `portfolio_items` columns:
     `title, brand_name, celebrity_name, category, image_url, thumbnail_url, show_on_web, show_on_pdf, sort_order`
   - use uploaded Blob URL for both `image_url` and `thumbnail_url`
   - set `sort_order` to `0`
   - return `{ success: true, data: { id, imageUrl } }`
2. `src/app/admin/upload/page.tsx`
   - render an authenticated upload form with file picker, title, brand name, celebrity name, category select, web/PDF checkboxes
   - run `resizePortfolioImage()` before sending the file
   - submit using `multipart/form-data` to `/api/upload`
   - show loading text, success text, and error text inline
   - include a preview of the selected image before submit

Use route-handler JSON errors and do not write directly to Blob or DB from the page component.
  </action>
  <verify>
    <automated>cd /Users/arisnoba/Documents/GitHub/afeelcompany && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/upload/route.ts` contains `await request.formData()`
    - `src/app/api/upload/route.ts` contains `INSERT INTO portfolio_items`
    - `src/app/api/upload/route.ts` contains `uploadPublicImage(file, 'portfolio')`
    - `src/app/admin/upload/page.tsx` contains `resizePortfolioImage`
    - `src/app/admin/upload/page.tsx` contains `multipart/form-data`
    - `src/app/admin/upload/page.tsx` contains `showOnWeb`
    - `src/app/admin/upload/page.tsx` contains `showOnPdf`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The admin can upload resized images with metadata, and the system stores both Blob URL and portfolio row.</done>
</task>

</tasks>

<verification>
Run:

```bash
cd /Users/arisnoba/Documents/GitHub/afeelcompany
npm run build
grep -n "portfolio_items" src/app/api/upload/route.ts
grep -n "resizePortfolioImage" src/app/admin/upload/page.tsx
```
</verification>

<success_criteria>
- authenticated admin upload endpoint exists
- client-side resize and inline progress UI exist
- Blob URL image rendering is supported in config
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-layer-admin/03-02-SUMMARY.md`
</output>
