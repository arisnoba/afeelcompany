---
phase: 03-data-layer-admin
plan: 04
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/app/api/company-profile/route.ts
  - src/app/api/client-brands/route.ts
  - src/app/api/client-brands/[id]/route.ts
  - src/app/admin/profile/page.tsx
  - src/components/admin/BrandManager.tsx
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03

must_haves:
  truths:
    - "`/admin/profile` can edit `company_profile.about_text`, `contact_email`, `contact_phone`, and `address`"
    - "`client_brands` rows can be created, deleted, and reordered from the admin UI"
    - "brand logo uploads go through the same authenticated Blob helper path"
    - "saving company profile data makes `/pdf-export` eligible to render real DB content instead of fixtures"
  artifacts:
    - path: "src/app/api/company-profile/route.ts"
      provides: "Singleton company profile read/update endpoint"
    - path: "src/app/api/client-brands/route.ts"
      provides: "Brand create/list endpoint"
    - path: "src/app/admin/profile/page.tsx"
      provides: "Profile and brand management page"
  key_links:
    - from: "src/app/api/company-profile/route.ts"
      to: "src/app/pdf-export/_lib/get-pdf-document.ts"
      via: "writes the fields that the PDF loader already reads"
      pattern: "about_text"
    - from: "src/app/api/client-brands/route.ts"
      to: "src/lib/blob.ts"
      via: "uploadPublicImage(file, 'brands')"
      pattern: "uploadPublicImage"
---

<objective>
Create the company profile and client brand management feature slice.

Purpose: Phase 2 already proved the PDF route. This plan is where real admin-managed company
copy and logo data start flowing into the same tables the PDF loader reads, replacing the
fixture path in real environments.

Output:
- `src/app/api/company-profile/route.ts`
- `src/app/api/client-brands/route.ts`
- `src/app/api/client-brands/[id]/route.ts`
- `src/app/admin/profile/page.tsx`
- `src/components/admin/BrandManager.tsx`
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-data-layer-admin/03-RESEARCH.md
@src/app/pdf-export/_lib/get-pdf-document.ts
@src/lib/blob.ts
@scripts/schema.sql

<interfaces>
company_profile_json:
  aboutText: string
  contactEmail: string
  contactPhone: string
  address: string
brand_form_fields:
  name: string
  sortOrder: string
  isActive: "true" | "false"
  logo: File
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement company profile read/update endpoint</name>
  <files>src/app/api/company-profile/route.ts</files>
  <read_first>
    - src/app/api/company-profile/route.ts
    - src/lib/auth.ts
    - src/lib/db.ts
    - src/app/pdf-export/_lib/get-pdf-document.ts
    - scripts/schema.sql
  </read_first>
  <action>
Create `src/app/api/company-profile/route.ts` with these exact behaviors:

1. `GET`
   - verify admin session
   - load the most recent row from `company_profile`
   - return `{ success: true, data: { aboutText, contactEmail, contactPhone, address } }`
2. `PUT`
   - verify admin session
   - accept JSON `{ aboutText, contactEmail, contactPhone, address }`
   - if a row exists, update it
   - if no row exists, insert one
   - persist to DB columns `about_text`, `contact_email`, `contact_phone`, `address`
   - return `{ success: true }`

Use a singleton-row approach. Do not create a new row on every update if one already exists.
  </action>
  <verify>
    <automated>cd /Users/arisnoba/Documents/GitHub/afeelcompany && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/company-profile/route.ts` contains `FROM company_profile`
    - `src/app/api/company-profile/route.ts` contains `about_text`
    - `src/app/api/company-profile/route.ts` contains `contact_email`
    - `src/app/api/company-profile/route.ts` contains `export async function PUT`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The admin can read and save the singleton company profile row that powers PDF/public data.</done>
</task>

<task type="auto">
  <name>Task 2: Implement client brand CRUD and the admin profile page</name>
  <files>src/app/api/client-brands/route.ts, src/app/api/client-brands/[id]/route.ts, src/app/admin/profile/page.tsx, src/components/admin/BrandManager.tsx</files>
  <read_first>
    - src/app/api/client-brands/route.ts
    - src/app/api/client-brands/[id]/route.ts
    - src/app/admin/profile/page.tsx
    - src/lib/blob.ts
    - scripts/schema.sql
    - src/components/ui/button.tsx
  </read_first>
  <action>
Create profile and brand management with these exact behaviors:

1. `src/app/api/client-brands/route.ts`
   - `GET` returns all rows ordered by `sort_order ASC, created_at ASC`
   - `POST` accepts `multipart/form-data` with `name`, `sortOrder`, `isActive`, `logo`
   - upload the logo through `uploadPublicImage(file, 'brands')`
   - insert `name`, `logo_url`, `sort_order`, `is_active`
2. `src/app/api/client-brands/[id]/route.ts`
   - `PATCH` updates `name`, `sortOrder`, `isActive`
   - `DELETE` removes Blob file first, then the DB row
3. `src/components/admin/BrandManager.tsx`
   - render brand list with name, logo preview, active toggle, sort order, delete button
4. `src/app/admin/profile/page.tsx`
   - render company profile form using `/api/company-profile`
   - render `BrandManager`
   - expose save feedback inline

Use field mapping `logo_url -> logoUrl`, `sort_order -> sortOrder`, `is_active -> isActive`.
  </action>
  <verify>
    <automated>cd /Users/arisnoba/Documents/GitHub/afeelcompany && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/client-brands/route.ts` contains `FROM client_brands`
    - `src/app/api/client-brands/route.ts` contains `uploadPublicImage(file, 'brands')`
    - `src/app/api/client-brands/[id]/route.ts` contains `export async function DELETE`
    - `src/components/admin/BrandManager.tsx` contains `sortOrder`
    - `src/app/admin/profile/page.tsx` contains `/api/company-profile`
    - `src/app/admin/profile/page.tsx` contains `BrandManager`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The admin can edit company profile copy and manage client brand logos from a single page.</done>
</task>

</tasks>

<verification>
Run:

```bash
cd /Users/arisnoba/Documents/GitHub/afeelcompany
npm run build
grep -n "company_profile" src/app/api/company-profile/route.ts
grep -n "client_brands" src/app/api/client-brands/route.ts
```
</verification>

<success_criteria>
- company profile singleton CRUD exists
- client brand CRUD and logo uploads exist
- `/admin/profile` manages both profile copy and brand list
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-layer-admin/03-04-SUMMARY.md`
</output>
