---
phase: 03-data-layer-admin
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/app/api/portfolio/route.ts
  - src/app/api/portfolio/[id]/route.ts
  - src/app/api/portfolio/reorder/route.ts
  - src/app/admin/portfolio/page.tsx
  - src/components/admin/PortfolioTable.tsx
autonomous: true
requirements:
  - PORT-01
  - PORT-02
  - PORT-03
  - PORT-04
  - PORT-05

must_haves:
  truths:
    - "`/admin/portfolio` lists portfolio rows with thumbnail, title, brand, and visibility state"
    - "`PATCH /api/portfolio/[id]` updates editable portfolio fields"
    - "`POST /api/portfolio/reorder` writes `sort_order` updates for multiple rows"
    - "`DELETE /api/portfolio/[id]` deletes both Blob file and DB row"
    - "web/PDF exposure toggles map directly to `show_on_web` and `show_on_pdf`"
  artifacts:
    - path: "src/app/admin/portfolio/page.tsx"
      provides: "Admin portfolio management page"
    - path: "src/components/admin/PortfolioTable.tsx"
      provides: "Portfolio admin table UI"
    - path: "src/app/api/portfolio/[id]/route.ts"
      provides: "Single-item patch/delete endpoint"
  key_links:
    - from: "src/app/admin/portfolio/page.tsx"
      to: "src/components/admin/PortfolioTable.tsx"
      via: "render the table component with fetched rows"
      pattern: "PortfolioTable"
    - from: "src/app/api/portfolio/[id]/route.ts"
      to: "src/lib/blob.ts"
      via: "delete the stored blob before removing the DB row"
      pattern: "del("
---

<objective>
Build the portfolio management surface: table view, inline or modal editing, visibility
toggles, reorder persistence, and delete behavior.

Purpose: uploading assets alone is not enough. The admin needs a stable workspace to curate
what appears on the web and PDF surfaces, and the data must remain consistent with the PDF
loader and future public pages.

Output:
- `src/app/api/portfolio/route.ts`
- `src/app/api/portfolio/[id]/route.ts`
- `src/app/api/portfolio/reorder/route.ts`
- `src/app/admin/portfolio/page.tsx`
- `src/components/admin/PortfolioTable.tsx`
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-data-layer-admin/03-RESEARCH.md
@.planning/phases/03-data-layer-admin/03-02-SUMMARY.md
@src/types/portfolio.ts
@src/lib/blob.ts
@scripts/schema.sql

<interfaces>
patch_portfolio_json:
  title: string
  brandName: string
  celebrityName: string | null
  category: string
  showOnWeb: boolean
  showOnPdf: boolean
  sortOrder: number
reorder_request_json:
  items:
    - id: string
      sortOrder: number
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create portfolio list and edit/delete endpoints</name>
  <files>src/app/api/portfolio/route.ts, src/app/api/portfolio/[id]/route.ts</files>
  <read_first>
    - src/app/api/portfolio/route.ts
    - src/app/api/portfolio/[id]/route.ts
    - src/lib/auth.ts
    - src/lib/db.ts
    - src/lib/blob.ts
    - src/types/portfolio.ts
    - scripts/schema.sql
  </read_first>
  <action>
Create portfolio management endpoints with these exact behaviors:

1. `src/app/api/portfolio/route.ts`
   - export `async function GET()`
   - verify admin session
   - select rows from `portfolio_items`
   - order by `sort_order ASC, created_at DESC`
   - return thumbnail, title, brand_name, celebrity_name, category, show_on_web, show_on_pdf, sort_order
2. `src/app/api/portfolio/[id]/route.ts`
   - export `async function PATCH(request: Request, ctx: RouteContext<'/api/portfolio/[id]'>)`
   - parse JSON body for `title`, `brandName`, `celebrityName`, `category`, `showOnWeb`, `showOnPdf`, `sortOrder`
   - update the matching `portfolio_items` row
   - export `async function DELETE(_request: Request, ctx: RouteContext<'/api/portfolio/[id]'>)`
   - load the current `image_url`
   - delete the Blob object through `del(imageUrl)`
   - delete the DB row
   - return `{ success: true }`

Every endpoint must reject unauthenticated requests before hitting Blob or SQL.
  </action>
  <verify>
    <automated>cd /Users/arisnoba/Documents/GitHub/afeelcompany && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/portfolio/route.ts` contains `FROM portfolio_items`
    - `src/app/api/portfolio/route.ts` contains `ORDER BY sort_order ASC`
    - `src/app/api/portfolio/[id]/route.ts` contains `export async function PATCH`
    - `src/app/api/portfolio/[id]/route.ts` contains `export async function DELETE`
    - `src/app/api/portfolio/[id]/route.ts` contains `del(`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Portfolio list, edit, and delete endpoints exist with auth checks and Blob cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: Build portfolio management page and reorder endpoint</name>
  <files>src/app/api/portfolio/reorder/route.ts, src/app/admin/portfolio/page.tsx, src/components/admin/PortfolioTable.tsx</files>
  <read_first>
    - src/app/admin/portfolio/page.tsx
    - src/components/admin/PortfolioTable.tsx
    - src/components/ui/button.tsx
    - src/components/ui/dialog.tsx
    - src/app/api/portfolio/route.ts
    - src/types/portfolio.ts
  </read_first>
  <action>
Create the portfolio admin UI with these exact capabilities:

1. `src/app/api/portfolio/reorder/route.ts`
   - export `async function POST(request: Request)`
   - verify admin session
   - accept JSON `{ items: [{ id, sortOrder }] }`
   - update `portfolio_items.sort_order` for each item
   - return `{ success: true }`
2. `src/components/admin/PortfolioTable.tsx`
   - render columns for thumbnail, title, brand, category, web toggle, PDF toggle, sort order, actions
   - provide edit controls for title/brand/celebrity/category
   - provide delete action
   - provide save action that calls `PATCH /api/portfolio/[id]`
3. `src/app/admin/portfolio/page.tsx`
   - fetch the portfolio rows on the server
   - render `PortfolioTable`
   - expose reorder controls that call `/api/portfolio/reorder`

Use exact DB-backed field names when mapping:
`brand_name -> brandName`, `show_on_web -> showOnWeb`, `show_on_pdf -> showOnPdf`, `sort_order -> sortOrder`.
  </action>
  <verify>
    <automated>cd /Users/arisnoba/Documents/GitHub/afeelcompany && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/portfolio/reorder/route.ts` contains `items: [{ id, sortOrder }]` or `sortOrder`
    - `src/components/admin/PortfolioTable.tsx` contains `showOnWeb`
    - `src/components/admin/PortfolioTable.tsx` contains `showOnPdf`
    - `src/components/admin/PortfolioTable.tsx` contains `/api/portfolio/`
    - `src/app/admin/portfolio/page.tsx` contains `PortfolioTable`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The admin can list, edit, toggle, reorder, and delete portfolio items from one management page.</done>
</task>

</tasks>

<verification>
Run:

```bash
cd /Users/arisnoba/Documents/GitHub/afeelcompany
npm run build
grep -n "sort_order" src/app/api/portfolio/reorder/route.ts
grep -n "showOnPdf" src/components/admin/PortfolioTable.tsx
```
</verification>

<success_criteria>
- portfolio management table exists
- edit/toggle/delete/reorder endpoints exist
- Blob deletion happens when an item is deleted
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-layer-admin/03-03-SUMMARY.md`
</output>
