---
phase: 04-public-site
plan: 05
type: execute
wave: 4
depends_on:
  - 04-02
  - 04-03
  - 04-04
autonomous: false
files_modified: []
requirements:
  - SITE-01
  - SITE-02
  - SITE-03
  - SITE-04
  - SITE-05
  - SITE-06
  - SITE-07
  - SITE-08

must_haves:
  truths:
    - "the full public site builds cleanly"
    - "HOME, ABOUT, PORTFOLIO, FEED, and CONTACT are all reachable under the shared public shell"
    - "portfolio category filtering and lightbox are manually verified"
    - "desktop and mobile layouts are manually reviewed across all public pages"
    - "the phase ends with either `approved` or a concrete issue list"
  artifacts:
    - path: "src/app/(public)/page.tsx"
      provides: "HOME route under verification"
    - path: "src/app/(public)/about/page.tsx"
      provides: "ABOUT route under verification"
    - path: "src/app/(public)/portfolio/page.tsx"
      provides: "PORTFOLIO route under verification"
    - path: "src/app/(public)/feed/page.tsx"
      provides: "FEED route under verification"
    - path: "src/app/(public)/contact/page.tsx"
      provides: "CONTACT route under verification"
---

<objective>
Run the public-site baseline and human visitor-facing checkpoint for Phase 4.

Purpose: Phase 4 success is not only "pages exist." The entire public site must be checked
as a connected, responsive visitor journey before Phase 5 integration and launch work begins.

Output: explicit approval or a defect list with exact failing pages/behaviors.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-public-site/04-VALIDATION.md
@.planning/phases/04-public-site/04-02-PLAN.md
@.planning/phases/04-public-site/04-03-PLAN.md
@.planning/phases/04-public-site/04-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the static public-site verification baseline</name>
  <files></files>
  <read_first>
    - .planning/phases/04-public-site/04-VALIDATION.md
  </read_first>
  <action>
Run the exact baseline commands before browser verification:

```bash
cd /Users/arisnoba/Documents/GitHub/afeelcompany
npm run lint
npm run build
```

If either command fails, stop and fix the implementation before manual verification.
  </action>
  <verify>
    <automated>cd /Users/arisnoba/Documents/GitHub/afeelcompany && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The public site has a green static baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Run the human public-site checkpoint across desktop and mobile</name>
  <files></files>
  <read_first>
    - src/app/(public)/page.tsx
    - src/app/(public)/about/page.tsx
    - src/app/(public)/portfolio/page.tsx
    - src/app/(public)/feed/page.tsx
    - src/app/(public)/contact/page.tsx
  </read_first>
  <action>
Run this exact manual checklist:

1. Open `/`
   - confirm header and footer are visible
   - confirm hero section renders
   - confirm latest portfolio preview renders or shows the empty-state message
2. Open `/about`
   - confirm company intro text is readable
   - confirm brand logos render or the empty-state message appears
3. Open `/portfolio`
   - click at least two category filters
   - open one portfolio item
   - confirm the lightbox shows image, title, brand, and category
   - close the lightbox
4. Open `/feed`
   - confirm cached grid renders or the empty-state message appears
   - confirm freshness copy is visible
5. Open `/contact`
   - confirm email/phone/address blocks render
   - click the email and phone links when values exist
6. Repeat the route review at a mobile viewport and a desktop viewport
   - confirm no route overflows horizontally
   - confirm navigation remains usable
   - confirm cards and grids reflow correctly

Record one of these final outcomes:
- `approved`
- `issues found` with each issue tied to `/`, `/about`, `/portfolio`, `/feed`, or `/contact`

Do not mark Phase 4 complete without an explicit human result.
  </action>
  <verify>
    <automated>echo "Manual checkpoint required"</automated>
  </verify>
  <acceptance_criteria>
    - home review completed
    - about review completed
    - portfolio review completed
    - feed review completed
    - contact review completed
    - mobile review completed
    - desktop review completed
    - final outcome is recorded as `approved` or a concrete issue list
  </acceptance_criteria>
  <done>The public site has an explicit visitor-facing sign-off result.</done>
</task>

</tasks>

<verification>
Automated baseline:

```bash
cd /Users/arisnoba/Documents/GitHub/afeelcompany
npm run lint && npm run build
```

Manual baseline:
- home review complete
- about review complete
- portfolio review complete
- feed review complete
- contact review complete
- mobile and desktop reviews complete
</verification>

<success_criteria>
- lint and build pass
- all five public pages are exercised in the browser
- responsive behavior is reviewed at mobile and desktop widths
- the final outcome is explicit before phase completion
</success_criteria>

<output>
After completion, create `.planning/phases/04-public-site/04-05-SUMMARY.md`
</output>
