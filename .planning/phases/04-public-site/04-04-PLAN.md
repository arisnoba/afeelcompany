---
phase: 04-public-site
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/app/(public)/feed/page.tsx
  - src/app/(public)/contact/page.tsx
  - src/components/site/InstagramFeedGrid.tsx
autonomous: true
requirements:
  - SITE-05
  - SITE-06
  - SITE-08

must_haves:
  truths:
    - "`src/app/(public)/feed/page.tsx` reads cached Instagram rows through `getCachedFeed()`"
    - "`src/components/site/InstagramFeedGrid.tsx` renders a responsive grid and an explicit empty-state message"
    - "`src/app/(public)/contact/page.tsx` reads company profile data through `getSiteCompanyProfile()`"
    - "CONTACT renders `mailto:` and `tel:` links when values exist"
  artifacts:
    - path: "src/app/(public)/feed/page.tsx"
      provides: "Visitor-facing Instagram feed route"
    - path: "src/components/site/InstagramFeedGrid.tsx"
      provides: "Cached Instagram feed grid renderer"
      exports: ["InstagramFeedGrid"]
    - path: "src/app/(public)/contact/page.tsx"
      provides: "Visitor-facing contact route"
  key_links:
    - from: "src/app/(public)/feed/page.tsx"
      to: "src/lib/instagram.ts"
      via: "import { getCachedFeed } from '@/lib/instagram'"
      pattern: "getCachedFeed"
    - from: "src/app/(public)/contact/page.tsx"
      to: "src/lib/site.ts"
      via: "import { getSiteCompanyProfile } from '@/lib/site'"
      pattern: "getSiteCompanyProfile"
---

<objective>
Build the FEED and CONTACT routes using the already-approved public data sources.

Purpose: this plan completes the remaining visitor pages without inventing new API paths.
The FEED route must use Instagram cache rows only, and CONTACT must expose company profile
details in a simple responsive layout.

Output:
- `src/app/(public)/feed/page.tsx`
- `src/app/(public)/contact/page.tsx`
- `src/components/site/InstagramFeedGrid.tsx`
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-public-site/04-RESEARCH.md
@.planning/phases/04-public-site/04-VALIDATION.md
@src/lib/instagram.ts
@src/types/instagram.ts
@src/lib/site.ts
@src/types/site.ts
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the FEED page from cached Instagram rows only</name>
  <files>src/app/(public)/feed/page.tsx, src/components/site/InstagramFeedGrid.tsx</files>
  <read_first>
    - src/app/admin/instagram-test/page.tsx
    - src/lib/instagram.ts
    - src/types/instagram.ts
    - next.config.ts
    - .planning/phases/04-public-site/04-RESEARCH.md
  </read_first>
  <action>
Create `src/components/site/InstagramFeedGrid.tsx` that accepts:

```ts
interface InstagramFeedGridProps {
  posts: CachedPost[]
}
```

and renders:
- a responsive grid of cached posts
- for non-video items: a `next/image` tile
- for video rows: a fallback tile with visible text `VIDEO`
- for empty data: visible text `동기화된 인스타그램 피드가 아직 없습니다.`

Create `src/app/(public)/feed/page.tsx` as an async server component with these exact behaviors:

1. load:
   ```ts
   const posts = await getCachedFeed()
   ```
2. derive:
   ```ts
   const lastSync = posts[0]?.fetched_at ?? null
   ```
3. render a page heading with visible text `FEED`
4. render freshness copy:
   - when `lastSync` exists, show visible text starting with `마지막 동기화:`
   - otherwise show visible text `캐시된 인스타그램 데이터가 아직 없습니다.`
5. render:
   ```tsx
   <InstagramFeedGrid posts={posts} />
   ```

Do not render a sync button or call `/api/instagram/sync` from the public route.
  </action>
  <verify>
    <automated>cd /Users/arisnoba/Documents/GitHub/afeelcompany && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(public)/feed/page.tsx` contains `await getCachedFeed()`
    - `src/app/(public)/feed/page.tsx` contains `마지막 동기화:`
    - `src/app/(public)/feed/page.tsx` contains `캐시된 인스타그램 데이터가 아직 없습니다.`
    - `src/components/site/InstagramFeedGrid.tsx` contains `VIDEO`
    - `src/components/site/InstagramFeedGrid.tsx` contains `동기화된 인스타그램 피드가 아직 없습니다.`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The public FEED page shows Instagram content strictly from the DB cache.</done>
</task>

<task type="auto">
  <name>Task 2: Build the CONTACT page from the shared company profile loader</name>
  <files>src/app/(public)/contact/page.tsx</files>
  <read_first>
    - src/app/(public)/contact/page.tsx
    - src/lib/site.ts
    - src/types/site.ts
    - .planning/phases/04-public-site/04-RESEARCH.md
  </read_first>
  <action>
Create `src/app/(public)/contact/page.tsx` as an async server component with these exact behaviors:

1. load:
   ```ts
   const profile = await getSiteCompanyProfile()
   ```
2. render a page heading with visible text `CONTACT`
3. render supporting copy with visible text `프로젝트 문의`
4. render the following fields in distinct blocks:
   - email
   - phone
   - address
5. when email exists, wrap it in:
   ```tsx
   href={`mailto:${profile.contactEmail}`}
   ```
6. when phone exists, wrap it in:
   ```tsx
   href={`tel:${profile.contactPhone}`}
   ```
7. when a field is empty, render the literal fallback text `정보를 준비 중입니다.`

Keep the layout responsive with a single-column stack on small screens and a two-column split on larger screens.
  </action>
  <verify>
    <automated>cd /Users/arisnoba/Documents/GitHub/afeelcompany && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(public)/contact/page.tsx` contains `await getSiteCompanyProfile()`
    - `src/app/(public)/contact/page.tsx` contains `CONTACT`
    - `src/app/(public)/contact/page.tsx` contains `프로젝트 문의`
    - `src/app/(public)/contact/page.tsx` contains `mailto:`
    - `src/app/(public)/contact/page.tsx` contains `tel:`
    - `src/app/(public)/contact/page.tsx` contains `정보를 준비 중입니다.`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The public CONTACT page exposes company contact details safely and responsively.</done>
</task>

</tasks>

<verification>
Run:

```bash
cd /Users/arisnoba/Documents/GitHub/afeelcompany
npm run build
grep -n "await getCachedFeed()" src/app/(public)/feed/page.tsx
grep -n "동기화된 인스타그램 피드가 아직 없습니다." src/components/site/InstagramFeedGrid.tsx
grep -n "mailto:" src/app/(public)/contact/page.tsx
grep -n "tel:" src/app/(public)/contact/page.tsx
```
</verification>

<success_criteria>
- FEED uses cached Instagram rows only
- CONTACT renders company profile fields with safe fallbacks
- both routes behave responsively
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-public-site/04-04-SUMMARY.md`
</output>
