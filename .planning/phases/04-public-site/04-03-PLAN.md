---
phase: 04-public-site
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/app/(public)/portfolio/page.tsx
  - src/components/site/PortfolioGalleryClient.tsx
  - src/components/site/PortfolioLightbox.tsx
autonomous: true
requirements:
  - SITE-03
  - SITE-04
  - SITE-08

must_haves:
  truths:
    - "`src/app/(public)/portfolio/page.tsx` loads portfolio items from `getPublicPortfolioItems()`"
    - "`src/components/site/PortfolioGalleryClient.tsx` renders an `전체` filter plus category filters derived from `PORTFOLIO_CATEGORIES`"
    - "`src/components/site/PortfolioLightbox.tsx` uses the existing dialog primitives from `@/components/ui/dialog`"
    - "Clicking a portfolio card can open a small lightbox showing image, title, brand, and category"
  artifacts:
    - path: "src/app/(public)/portfolio/page.tsx"
      provides: "Visitor-facing portfolio route"
    - path: "src/components/site/PortfolioGalleryClient.tsx"
      provides: "Category filter + gallery interaction"
      exports: ["PortfolioGalleryClient"]
    - path: "src/components/site/PortfolioLightbox.tsx"
      provides: "Reusable portfolio lightbox modal"
      exports: ["PortfolioLightbox"]
  key_links:
    - from: "src/app/(public)/portfolio/page.tsx"
      to: "src/lib/site.ts"
      via: "import { getPublicPortfolioItems } from '@/lib/site'"
      pattern: "getPublicPortfolioItems"
    - from: "src/components/site/PortfolioGalleryClient.tsx"
      to: "src/types/portfolio.ts"
      via: "import { PORTFOLIO_CATEGORIES } from '@/types/portfolio'"
      pattern: "PORTFOLIO_CATEGORIES"
    - from: "src/components/site/PortfolioLightbox.tsx"
      to: "src/components/ui/dialog.tsx"
      via: "import { Dialog, DialogContent, DialogDescription, DialogTitle } from '@/components/ui/dialog'"
      pattern: "@/components/ui/dialog"
---

<objective>
Build the public PORTFOLIO page with category filters and a small lightbox modal.

Purpose: SITE-03 and SITE-04 are the most interaction-heavy public requirements. This plan
implements the complete gallery flow while keeping data loading server-side and limiting
client code to filter/modal state.

Output:
- `src/app/(public)/portfolio/page.tsx`
- `src/components/site/PortfolioGalleryClient.tsx`
- `src/components/site/PortfolioLightbox.tsx`
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-public-site/04-RESEARCH.md
@.planning/phases/04-public-site/04-VALIDATION.md
@src/lib/site.ts
@src/types/site.ts
@src/types/portfolio.ts
@src/components/ui/dialog.tsx
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the server portfolio route around getPublicPortfolioItems()</name>
  <files>src/app/(public)/portfolio/page.tsx</files>
  <read_first>
    - src/app/(public)/portfolio/page.tsx
    - src/lib/site.ts
    - src/types/site.ts
    - .planning/phases/04-public-site/04-RESEARCH.md
  </read_first>
  <action>
Create `src/app/(public)/portfolio/page.tsx` as an async server component with these exact behaviors:

1. load:
   ```ts
   const items = await getPublicPortfolioItems()
   ```
2. render a page heading with visible text `PORTFOLIO`
3. render supporting copy with visible text `카테고리별 포트폴리오`
4. render:
   ```tsx
   <PortfolioGalleryClient items={items} />
   ```
5. if there are zero items, still render the page heading and let the client component show the empty-state message

Do not fetch `/api/portfolio`. Use the shared loader from `src/lib/site.ts`.
  </action>
  <verify>
    <automated>cd /Users/arisnoba/Documents/GitHub/afeelcompany && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(public)/portfolio/page.tsx` contains `await getPublicPortfolioItems()`
    - `src/app/(public)/portfolio/page.tsx` contains `PORTFOLIO`
    - `src/app/(public)/portfolio/page.tsx` contains `카테고리별 포트폴리오`
    - `src/app/(public)/portfolio/page.tsx` contains `PortfolioGalleryClient`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The visitor-facing portfolio route exists and receives server-loaded portfolio data.</done>
</task>

<task type="auto">
  <name>Task 2: Implement category filtering and the portfolio lightbox client island</name>
  <files>src/components/site/PortfolioGalleryClient.tsx, src/components/site/PortfolioLightbox.tsx</files>
  <read_first>
    - src/components/site/PortfolioGalleryClient.tsx
    - src/components/site/PortfolioLightbox.tsx
    - src/components/ui/dialog.tsx
    - src/types/portfolio.ts
    - src/types/site.ts
  </read_first>
  <action>
Create `src/components/site/PortfolioGalleryClient.tsx` as a client component with these exact behaviors:

1. accept props:
   ```ts
   interface PortfolioGalleryClientProps {
     items: PublicPortfolioItem[]
   }
   ```
2. derive filter options from:
   - the literal label `전체`
   - `PORTFOLIO_CATEGORIES`
3. keep local state:
   - `selectedCategory`
   - `activeIndex`
4. filter rule:
   - if `selectedCategory === '전체'`, show all items
   - otherwise show only matching `item.category`
5. render:
   - one button per filter
   - a responsive image grid
   - an empty-state message `선택한 카테고리에 해당하는 작업이 없습니다.` when the filtered list is empty
6. clicking a card sets `activeIndex` and opens the lightbox

Create `src/components/site/PortfolioLightbox.tsx` as a client component that:

1. imports exactly from `@/components/ui/dialog`
2. accepts:
   ```ts
   interface PortfolioLightboxProps {
     item: PublicPortfolioItem | null
     open: boolean
     onOpenChange: (open: boolean) => void
   }
   ```
3. renders a dialog with:
   - the portfolio image
   - title
   - brand name
   - celebrity name when present
   - category
4. uses:
   - `Dialog`
   - `DialogContent`
   - `DialogTitle`
   - `DialogDescription`

Keep the modal compact rather than full-screen. Do not fetch extra data when a card is clicked.
  </action>
  <verify>
    <automated>cd /Users/arisnoba/Documents/GitHub/afeelcompany && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/site/PortfolioGalleryClient.tsx` contains `use client`
    - `src/components/site/PortfolioGalleryClient.tsx` contains `const FILTER_ALL = '전체'`
    - `src/components/site/PortfolioGalleryClient.tsx` contains `PORTFOLIO_CATEGORIES`
    - `src/components/site/PortfolioGalleryClient.tsx` contains `selectedCategory === '전체'`
    - `src/components/site/PortfolioGalleryClient.tsx` contains `선택한 카테고리에 해당하는 작업이 없습니다.`
    - `src/components/site/PortfolioLightbox.tsx` contains `DialogContent`
    - `src/components/site/PortfolioLightbox.tsx` contains `DialogTitle`
    - `src/components/site/PortfolioLightbox.tsx` contains `DialogDescription`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The portfolio page supports category filtering and a working small lightbox modal.</done>
</task>

</tasks>

<verification>
Run:

```bash
cd /Users/arisnoba/Documents/GitHub/afeelcompany
npm run build
grep -n "PORTFOLIO_CATEGORIES" src/components/site/PortfolioGalleryClient.tsx
grep -n "DialogContent" src/components/site/PortfolioLightbox.tsx
grep -n "selectedCategory === '전체'" src/components/site/PortfolioGalleryClient.tsx
```
</verification>

<success_criteria>
- portfolio page loads shared public items
- category filter works from canonical category labels
- clicking a card opens a small modal with item details
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-public-site/04-03-SUMMARY.md`
</output>
