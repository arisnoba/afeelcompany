---
phase: 02-spike-pdf
plan: 05
type: execute
wave: 5
depends_on:
  - 02-04
autonomous: false
files_modified: []
requirements:
  - PDF-01
  - PDF-02
  - PDF-03
  - PDF-04
  - PDF-05
  - PDF-06

must_haves:
  truths:
    - "Chrome prints `/pdf-export?print=1` as an acceptable A4-landscape brochure"
    - "Safari prints `/pdf-export?print=1` with the same brochure order and without broken Korean text"
    - "The route shows `cover -> ABOUT -> WORK -> CLIENT -> CONTACT` in both browsers"
    - "The `다운로드` action triggers the auto-print flow"
    - "Any remaining print defects are recorded explicitly before the phase is marked complete"
  artifacts:
    - path: "src/app/pdf-export/page.tsx"
      provides: "Brochure route under verification"
    - path: "src/app/pdf-export/print.css"
      provides: "Print rules under verification"
  key_links:
    - from: "Browser print dialog"
      to: "src/app/pdf-export/_components/AutoPrintOnLoad.tsx"
      via: "window.print() after fonts/images load"
      pattern: "window.print()"
---

<objective>
Run the human browser checkpoint that decides whether the browser-print approach is good
enough to keep for MVP.

Purpose: this is the actual spike gate. Until Chrome and Safari produce acceptable brochure
output, the team has not answered the roadmap question for Phase 2.

Output: human approval or a concrete defect list with browser-specific findings.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-spike-pdf/02-03-SUMMARY.md
@.planning/phases/02-spike-pdf/02-04-SUMMARY.md
@.planning/phases/02-spike-pdf/02-VALIDATION.md
@docs/어필컴퍼니 회사소개서 VER.2.pdf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification baseline before manual browser review</name>
  <files></files>
  <read_first>
    - .planning/phases/02-spike-pdf/02-VALIDATION.md
    - src/app/pdf-export/page.tsx
    - src/app/pdf-export/print.css
  </read_first>
  <action>
Before opening browsers, run:

```bash
cd /Users/arisnoba/Documents/GitHub/afeelcompany
npm run lint
npm run build
```

If either command fails, stop the checkpoint and fix implementation defects first.
  </action>
  <verify>
    <automated>cd /Users/arisnoba/Documents/GitHub/afeelcompany && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Static verification baseline is green and the browser checkpoint can start.</done>
</task>

<task type="auto">
  <name>Task 2: Human browser checkpoint in Chrome and Safari</name>
  <files></files>
  <read_first>
    - docs/어필컴퍼니 회사소개서 VER.2.pdf
    - src/app/pdf-export/page.tsx
    - src/app/pdf-export/print.css
  </read_first>
  <action>
Run this exact manual checklist:

1. Start the app locally.
2. Open `/pdf-export` in Chrome and confirm:
   - the brochure renders on screen
   - section order is `cover -> ABOUT -> WORK -> CLIENT -> CONTACT`
   - at least four work images are visible
3. Open `/pdf-export?print=1` in Chrome and confirm:
   - the print dialog opens automatically
   - landscape orientation is correct
   - Korean text is readable
   - no work image tile is blank or clipped across pages
4. Save the Chrome PDF and compare it against the current brochure reference in `docs/`.
5. Repeat steps 2-4 in Safari.
6. Record one of these outcomes:
   - `approved` - browser print is acceptable for MVP
   - `issues found` - list each issue with browser name, page/section, and observed defect

Do not mark the phase complete without an explicit human outcome.
  </action>
  <verify>
    <automated>echo "Manual checkpoint required"</automated>
  </verify>
  <acceptance_criteria>
    - Chrome route review completed
    - Chrome print review completed
    - Safari route review completed
    - Safari print review completed
    - final outcome is recorded as either `approved` or a concrete issue list
  </acceptance_criteria>
  <done>The spike has a human sign-off result for browser-print viability.</done>
</task>

</tasks>

<verification>
Automated baseline:

```bash
cd /Users/arisnoba/Documents/GitHub/afeelcompany
npm run lint && npm run build
```

Manual baseline:
- Chrome review complete
- Safari review complete
- saved output visually acceptable or issues explicitly listed
</verification>

<success_criteria>
- lint and build pass
- Chrome and Safari both reviewed
- the MVP decision about browser print is explicit
</success_criteria>

<output>
After completion, create `.planning/phases/02-spike-pdf/02-05-SUMMARY.md`
</output>
