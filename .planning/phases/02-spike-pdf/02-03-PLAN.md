---
phase: 02-spike-pdf
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/app/pdf-export/page.tsx
  - src/app/pdf-export/_components/BrochureSheet.tsx
autonomous: true
requirements:
  - PDF-01
  - PDF-03
  - PDF-04

must_haves:
  truths:
    - "Visiting `/pdf-export` renders brochure sheets in the exact order `cover -> ABOUT -> WORK -> CLIENT -> CONTACT`"
    - "`src/app/pdf-export/page.tsx` loads brochure data through `getPdfDocument()`"
    - "The WORK section renders at least four brochure image tiles from `workItems`"
    - "The CLIENT section renders brand logo tiles from `clientBrands`"
    - "Korean text content appears in ABOUT and CONTACT sections"
  artifacts:
    - path: "src/app/pdf-export/page.tsx"
      provides: "The brochure route server component"
    - path: "src/app/pdf-export/_components/BrochureSheet.tsx"
      provides: "Reusable sheet wrapper for brochure pages"
      exports: ["BrochureSheet"]
  key_links:
    - from: "src/app/pdf-export/page.tsx"
      to: "src/app/pdf-export/_lib/get-pdf-document.ts"
      via: "import { getPdfDocument } from './_lib/get-pdf-document'"
      pattern: "getPdfDocument"
    - from: "src/app/pdf-export/page.tsx"
      to: "src/app/pdf-export/_components/AutoPrintOnLoad.tsx (Plan 04)"
      via: "render AutoPrintOnLoad above the brochure sheets"
      pattern: "AutoPrintOnLoad"
---

<objective>
Build the brochure route itself: fetch the brochure document, compose explicit brochure
pages, and render the section order required by PDF-03.

Purpose: this plan creates the actual `/pdf-export` HTML that later print CSS and the
auto-print launcher will operate on.

Output:
- `src/app/pdf-export/page.tsx`
- `src/app/pdf-export/_components/BrochureSheet.tsx`
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-spike-pdf/02-RESEARCH.md
@.planning/phases/02-spike-pdf/02-02-SUMMARY.md
@src/app/pdf-export/_lib/get-pdf-document.ts
@src/types/pdf.ts

<interfaces>
export async function getPdfDocument(): Promise<PdfDocument>

export interface PdfDocument {
  title: string
  issueDate: string
  heroImageUrl: string
  aboutText: string
  contact: PdfContact
  workItems: PdfPortfolioItem[]
  clientBrands: PdfClientBrand[]
  sectionOrder: PdfSectionId[]
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the brochure sheet wrapper component</name>
  <files>src/app/pdf-export/_components/BrochureSheet.tsx</files>
  <read_first>
    - src/app/pdf-export/_components/BrochureSheet.tsx
    - .planning/phases/02-spike-pdf/02-RESEARCH.md
  </read_first>
  <action>
Create `src/app/pdf-export/_components/BrochureSheet.tsx` that exports:

```tsx
interface BrochureSheetProps {
  sectionId: 'cover' | 'about' | 'work' | 'client' | 'contact'
  title?: string
  children: React.ReactNode
}

export function BrochureSheet({ sectionId, title, children }: BrochureSheetProps) { ... }
```

The rendered wrapper must include:
- `data-section={sectionId}`
- class names `pdf-sheet` and `pdf-sheet--${sectionId}`
- an optional heading area when `title` is provided
- a content container that fills the sheet body

Do not add print rules here; leave that to Plan 04.
  </action>
  <verify>
    <automated>cd /Users/arisnoba/Documents/GitHub/afeelcompany && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/pdf-export/_components/BrochureSheet.tsx` exists
    - `src/app/pdf-export/_components/BrochureSheet.tsx` contains `export function BrochureSheet`
    - `src/app/pdf-export/_components/BrochureSheet.tsx` contains `data-section={sectionId}`
    - `src/app/pdf-export/_components/BrochureSheet.tsx` contains `pdf-sheet`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The brochure page wrapper exists and gives Plan 04 stable hooks for print CSS.</done>
</task>

<task type="auto">
  <name>Task 2: Build src/app/pdf-export/page.tsx with the required brochure order</name>
  <files>src/app/pdf-export/page.tsx</files>
  <read_first>
    - src/app/pdf-export/page.tsx
    - src/app/pdf-export/_lib/get-pdf-document.ts
    - src/app/pdf-export/_components/BrochureSheet.tsx
    - src/app/pdf-export/_lib/pdf-fixtures.ts
    - src/types/pdf.ts
  </read_first>
  <action>
Create `src/app/pdf-export/page.tsx` as an async server component with these exact behaviors:

1. Export:
   ```ts
   export const dynamic = 'force-dynamic'
   ```
2. Call:
   ```ts
   const brochure = await getPdfDocument()
   ```
3. Render five `BrochureSheet` blocks in this exact order:
   - `cover`
   - `about`
   - `work`
   - `client`
   - `contact`
4. The cover sheet must show:
   - `brochure.title`
   - `brochure.issueDate`
   - the hero image from `brochure.heroImageUrl`
5. The ABOUT sheet must show:
   - heading text `ABOUT`
   - `brochure.aboutText`
6. The WORK sheet must render a grid from `brochure.workItems.slice(0, 4)` and for each item show:
   - image
   - title
   - brand name
7. The CLIENT sheet must render a grid from `brochure.clientBrands`
8. The CONTACT sheet must show:
   - heading text `CONTACT`
   - `brochure.contact.email`
   - `brochure.contact.phone`
   - `brochure.contact.address`
9. Mark every brochure image element with `data-pdf-image`

Use Korean copy where the fixture already provides it. If an image path is missing, render an explicit fallback box that contains the text `IMAGE MISSING`.
  </action>
  <verify>
    <automated>cd /Users/arisnoba/Documents/GitHub/afeelcompany && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/pdf-export/page.tsx` contains `export const dynamic = 'force-dynamic'`
    - `src/app/pdf-export/page.tsx` contains `await getPdfDocument()`
    - `src/app/pdf-export/page.tsx` contains `ABOUT`
    - `src/app/pdf-export/page.tsx` contains `CONTACT`
    - `src/app/pdf-export/page.tsx` contains `data-pdf-image`
    - `src/app/pdf-export/page.tsx` contains `brochure.workItems.slice(0, 4)`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The `/pdf-export` page renders the brochure structure and all required sections from real or fixture data.</done>
</task>

</tasks>

<verification>
Run:

```bash
cd /Users/arisnoba/Documents/GitHub/afeelcompany
npm run build
```

Then confirm the route file encodes the required order:

```bash
grep -n "ABOUT" src/app/pdf-export/page.tsx
grep -n "CONTACT" src/app/pdf-export/page.tsx
grep -n "workItems.slice(0, 4)" src/app/pdf-export/page.tsx
```
</verification>

<success_criteria>
- `/pdf-export` is implemented as a dynamic route
- brochure page order matches PDF-03 exactly
- work and client sections render from brochure data
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-spike-pdf/02-03-SUMMARY.md`
</output>
